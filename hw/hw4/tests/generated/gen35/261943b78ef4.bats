load ../../harness

@test "261943b78ef4" {
  check 'if (-3    =     -4 -z  ∧4     *     Hp     <     x     +    y)      then     skip else   

 z :=   4     *mg   ' '⇒ z := (4*mg), {}
⇒ skip, {z → 0}'
}
