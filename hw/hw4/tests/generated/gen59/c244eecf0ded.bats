load ../../harness

@test "c244eecf0ded" {
  check 'if (A     *  -3  =    K-   2   ∧     0    -x  = W  +    y)      then   
  y  := x *z    +E6 else   

z    := z*     z    ' '⇒ z := (z*z), {}
⇒ skip, {z → 0}'
}
