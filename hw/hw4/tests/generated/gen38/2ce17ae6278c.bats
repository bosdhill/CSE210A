load ../../harness

@test "2ce17ae6278c" {
  check 'if (¬(z   =    x     *    2))      then skip      else  

 
z     := z  +     4    ;u:=  3*  3' '⇒ z := (z+4); u := (3*3), {}
⇒ skip; u := (3*3), {z → 4}
⇒ u := (3*3), {z → 4}
⇒ skip, {u → 9, z → 4}'
}
