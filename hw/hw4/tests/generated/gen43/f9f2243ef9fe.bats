load ../../harness

@test "f9f2243ef9fe" {
  check 'if (-4+    f9     < -2    +     G   ∨    z    +   z= -1     -     nr)   then 


  z :=     x* 0     else 
 skip ' '⇒ z := (x*0), {}
⇒ skip, {z → 0}'
}
