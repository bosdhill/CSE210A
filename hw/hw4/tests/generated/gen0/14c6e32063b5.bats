load ../../harness

@test "14c6e32063b5" {
  check 'while 0-    1<   es*aq ∧ true      do   z  :=  -1 + y' '⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ skip; while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}
⇒ z := (-1+y); while (((0-1)<(es*aq))∧true) do { z := (-1+y) }, {z → -1}'
}
