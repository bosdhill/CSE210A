load ../../harness

@test "aa044738135f" {
  check 'if (x     -     1 =-1  * -3     ∧-1     -     -2  <     x-  (3+   3))   then skip   else   mu   :=     RX    +y     ' '⇒ mu := (RX+y), {}
⇒ skip, {mu → 0}'
}
