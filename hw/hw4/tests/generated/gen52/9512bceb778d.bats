load ../../harness

@test "9512bceb778d" {
  check 'if (1     *y  < z     +   K     ∧     -2     -     x     =     x   -   -1)      then 


skip   else 
 x     := z+   y     ' '⇒ x := (z+y), {}
⇒ skip, {x → 0}'
}
