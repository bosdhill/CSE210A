load ../../harness

@test "9efc1dab986e" {
  check 'if (-1  -  -3   <   x     -     0     ∨z  -  y     < y     +2)     then Q3    :=  y*  -2   else 
   skip  ' '⇒ Q3 := (y*-2), {}
⇒ skip, {Q3 → 0}'
}
