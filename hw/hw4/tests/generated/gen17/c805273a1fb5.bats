load ../../harness

@test "c805273a1fb5" {
  check 'if (x    =     z    *     y  ∧     -3   + -3    <   h *  -2)      then 
  
 k   :=     n   +z   else 
    b  := -3  -o5 ' '⇒ k := (n+z), {}
⇒ skip, {k → 0}'
}
