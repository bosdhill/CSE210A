load ../../harness

@test "517bdd680eb3" {
  check 'PU:=     2 -  -2; 
 
 skip     ;   Hd  := z*   X ' '⇒ skip; skip; Hd := (z*X), {PU → 4}
⇒ skip; Hd := (z*X), {PU → 4}
⇒ Hd := (z*X), {PU → 4}
⇒ skip, {Hd → 0, PU → 4}'
}
