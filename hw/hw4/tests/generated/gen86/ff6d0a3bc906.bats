load ../../harness

@test "ff6d0a3bc906" {
  check 'if (z     +T    =-3     *     -1     ∨   2  * -4=     4 *x)   then 

y  := z   *  0   else 
  
s    := 2     -z    ' '⇒ s := (2-z), {}
⇒ skip, {s → 2}'
}
