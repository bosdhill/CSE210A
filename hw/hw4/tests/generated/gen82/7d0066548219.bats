load ../../harness

@test "7d0066548219" {
  check 'x:= x   * y   ;
 

while false   ∨   3     + -4 =   y    *     y      do  

 
X     := y    -   1' '⇒ skip; while (false∨((3+-4)=(y*y))) do { X := (y-1) }, {x → 0}
⇒ while (false∨((3+-4)=(y*y))) do { X := (y-1) }, {x → 0}
⇒ skip, {x → 0}'
}
