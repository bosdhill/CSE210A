load ../../harness

@test "04e6195a2a62" {
  check 'y := 2*   2  ;
   
while -1     *(x-  0)<  x    *   se ∧true    do 
  
skip  ' '⇒ skip; while (((-1*(x-0))<(x*se))∧true) do { skip }, {y → 4}
⇒ while (((-1*(x-0))<(x*se))∧true) do { skip }, {y → 4}
⇒ skip, {y → 4}'
}
