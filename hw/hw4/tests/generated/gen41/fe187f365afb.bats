load ../../harness

@test "fe187f365afb" {
  check 'if (s    +  x =  x* 0    ∨4    *   y    <     y   *     z)     then yM :=     3   * FZ      else 
 
  skip   ' '⇒ yM := (3*FZ), {}
⇒ skip, {yM → 0}'
}
