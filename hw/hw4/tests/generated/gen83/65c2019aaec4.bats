load ../../harness

@test "65c2019aaec4" {
  check 'skip  ;

 
if (4*   z =y     - -2   ∨ y    + z=  a     +   y)      then skip   else skip    ' '⇒ if (((4*z)=(y--2))∨((y+z)=(a+y))) then { skip } else { skip }, {}
⇒ skip, {}'
}
