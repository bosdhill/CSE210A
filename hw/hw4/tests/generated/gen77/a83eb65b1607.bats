load ../../harness

@test "a83eb65b1607" {
  check 'if (x     +    1    <z  -     Kx   ∨    -2 +   I   <     -3     +    y)    then 

WP := -3*  -4  else  
  z  :=  k ' '⇒ z := k, {}
⇒ skip, {z → 0}'
}
